<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
<title>서우 여드름 짜기 챌린지</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Gaegu:wght@400;700&family=Noto+Sans+KR:wght@400;500;700;900&display=swap" rel="stylesheet">
<style>
  :root {
    --skin: #ffd9b3;
    --skin-shade: #f4bd85;
    --blush: #ff6b81;
    --blush-dark: #e14a63;
    --mint-bg: #eaf7f0;
    --mint-deep: #bfe8d4;
    --plum: #3a2e39;
    --yellow: #ffd23f;
    --white: #fffaf3;
  }
  * { box-sizing: border-box; }
  html, body { margin: 0; padding: 0; }
  body {
    min-height: 100vh;
    background:
      radial-gradient(circle at 15% 20%, var(--mint-deep) 0%, transparent 40%),
      radial-gradient(circle at 85% 80%, #ffe3b0 0%, transparent 45%),
      var(--mint-bg);
    font-family: "Noto Sans KR", sans-serif;
    color: var(--plum);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow-x: hidden;
  }
  #app {
    width: 100%;
    max-width: 480px;
    min-height: 100vh;
    padding: 24px 20px 40px;
    display: flex;
    flex-direction: column;
    align-items: center;
    position: relative;
  }
  .eyebrow {
    font-family: "Gaegu", sans-serif;
    font-weight: 700;
    font-size: 16px;
    letter-spacing: 2px;
    color: var(--blush-dark);
    text-transform: uppercase;
    margin-bottom: 4px;
  }
  h1 {
    font-family: "Gaegu", sans-serif;
    font-size: 36px;
    font-weight: 700;
    margin: 0 0 4px;
    text-align: center;
    line-height: 1.2;
  }
  .sub {
    font-size: 14px;
    color: #6b5c68;
    text-align: center;
    margin: 0 0 18px;
    max-width: 340px;
  }
  .screen { width: 100%; display: flex; flex-direction: column; align-items: center; }
  [hidden] { display: none !important; }

  /* Mode tabs */
  .mode-tabs {
    width: 100%;
    display: flex;
    gap: 8px;
    margin-bottom: 14px;
    background: rgba(58, 46, 57, 0.06);
    padding: 5px;
    border-radius: 16px;
  }
  .mode-tab {
    flex: 1;
    border: none;
    background: transparent;
    color: #8a7c86;
    font-family: "Gaegu", sans-serif;
    font-size: 18px;
    font-weight: 700;
    padding: 10px 4px;
    border-radius: 12px;
    cursor: pointer;
    transition: all 0.2s ease;
  }
  .mode-tab.active {
    background: var(--white);
    color: var(--blush-dark);
    box-shadow: 0 3px 0 rgba(58, 46, 57, 0.08);
  }

  /* Cards & Inputs */
  .card {
    width: 100%;
    background: var(--white);
    border-radius: 28px;
    padding: 22px;
    box-shadow: 0 14px 0 rgba(58, 46, 57, 0.06), 0 4px 18px rgba(58, 46, 57, 0.08);
    border: 3px solid rgba(58, 46, 57, 0.06);
  }
  label {
    display: block;
    font-weight: 700;
    font-size: 13px;
    color: var(--blush-dark);
    margin-bottom: 8px;
  }
  textarea, input[type="text"] {
    width: 100%;
    border-radius: 16px;
    border: 2px solid #f0e2d6;
    padding: 12px 14px;
    font-family: "Noto Sans KR", sans-serif;
    font-size: 14px;
    background: #fffdf9;
    color: var(--plum);
    outline: none;
    transition: border-color .15s ease;
  }
  textarea { min-height: 80px; resize: vertical; }
  textarea:focus, input[type="text"]:focus { border-color: var(--blush); }

  .presets {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin: 12px 0 4px;
  }
  .preset-btn {
    border: none;
    background: var(--mint-deep);
    color: #1f4e37;
    font-size: 12px;
    font-weight: 700;
    padding: 8px 12px;
    border-radius: 999px;
    cursor: pointer;
    transition: transform 0.1s ease, background 0.15s ease;
  }
  .preset-btn:hover { filter: brightness(0.95); }
  .preset-btn.active {
    background: var(--blush-dark);
    color: var(--white);
  }

  .primary-btn {
    width: 100%;
    margin-top: 16px;
    border: none;
    background: var(--blush);
    color: white;
    font-family: "Gaegu", sans-serif;
    font-size: 22px;
    font-weight: 700;
    padding: 14px;
    border-radius: 18px;
    cursor: pointer;
    box-shadow: 0 6px 0 var(--blush-dark);
    transition: transform 0.08s ease, box-shadow 0.08s ease;
  }
  .primary-btn:active { transform: translateY(4px); box-shadow: 0 2px 0 var(--blush-dark); }
  .primary-btn:disabled { opacity: 0.6; cursor: default; }
  .primary-btn.secondary {
    background: var(--yellow);
    color: var(--plum);
    box-shadow: 0 6px 0 #cf9f00;
  }
  .primary-btn.secondary:active { box-shadow: 0 2px 0 #cf9f00; }

  .status-line {
    font-size: 12px;
    color: #8a7c86;
    text-align: center;
    margin-top: 10px;
    min-height: 16px;
    font-weight: 500;
  }

  /* API Key Config Accordion */
  .api-key-toggle {
    margin-top: 12px;
    font-size: 12px;
    color: #8a7c86;
    text-decoration: underline;
    cursor: pointer;
    text-align: center;
    display: block;
  }
  .api-key-box {
    margin-top: 10px;
    padding: 10px;
    background: #f8f1ea;
    border-radius: 12px;
  }

  /* HUD */
  .hud {
    width: 100%;
    display: flex;
    justify-content: space-between;
    gap: 10px;
    margin-bottom: 14px;
  }
  .pill {
    flex: 1;
    background: var(--white);
    border-radius: 16px;
    padding: 10px 6px;
    text-align: center;
    box-shadow: 0 4px 0 rgba(58, 46, 57, 0.07);
  }
  .pill .num {
    font-family: "Gaegu", sans-serif;
    font-size: 28px;
    font-weight: 700;
    display: block;
    line-height: 1;
  }
  .pill .lbl {
    font-size: 11px;
    color: #8a7c86;
    font-weight: 700;
  }
  .pill.timer .num { color: var(--blush-dark); }
  .pill.score .num { color: #1f4e37; }
  .pill.combo .num { color: #a3720b; }

  /* Face stage */
  .stage-wrap {
    width: 100%;
    background: var(--white);
    border-radius: 28px;
    padding: 14px;
    box-shadow: 0 14px 0 rgba(58, 46, 57, 0.06), 0 4px 18px rgba(58, 46, 57, 0.08);
    border: 3px solid rgba(58, 46, 57, 0.06);
    position: relative;
  }
  #stage {
    position: relative;
    width: 100%;
    aspect-ratio: 3 / 4;
    border-radius: 22px;
    overflow: hidden;
    background: linear-gradient(180deg, #fff3e2, #ffe6c9);
    cursor: default;
    touch-action: manipulation;
    user-select: none;
  }

  /* Face Graphics */
  .face {
    position: absolute;
    inset: 0;
    z-index: 1;
    pointer-events: none;
  }
  .head {
    position: absolute;
    left: 50%; top: 52%;
    transform: translate(-50%, -50%);
    width: 66%;
    aspect-ratio: 1/1.08;
    background: var(--skin);
    border-radius: 48% 48% 42% 42% / 54% 54% 44% 44%;
    box-shadow: inset 0 -20px 32px rgba(244, 189, 133, 0.65), inset 0 14px 20px rgba(255, 255, 255, 0.5);
  }
  .ear {
    position: absolute;
    top: 48%;
    width: 9%;
    aspect-ratio: 1/1.3;
    background: var(--skin);
    border-radius: 50%;
    z-index: -1;
  }
  .ear.l { left: 14.5%; }
  .ear.r { right: 14.5%; }
  .neck {
    position: absolute;
    left: 50%; bottom: 10%;
    transform: translateX(-50%);
    width: 24%;
    height: 14%;
    background: var(--skin-shade);
    z-index: 0;
  }
  .blush-spot {
    position: absolute;
    top: 57%;
    width: 14%;
    height: 6%;
    background: rgba(255, 107, 129, 0.25);
    border-radius: 50%;
    filter: blur(4px);
  }
  .blush-spot.l { left: 24%; }
  .blush-spot.r { right: 24%; }

  .hair-wrap {
    position: absolute;
    left: 50%; top: 5%;
    transform: translateX(-50%);
    width: 78%;
    aspect-ratio: 1/1.02;
    clip-path: polygon(
      0% 34%, 6% 17%, 16% 6%, 30% 1%, 44% 5%, 50% 15%, 56% 5%, 70% 1%, 84% 6%, 94% 17%, 100% 34%,
      100% 58%, 90% 47%, 82% 60%, 73% 44%, 64% 58%, 55% 41%, 46% 57%,
      37% 41%, 28% 58%, 19% 44%, 10% 60%, 2% 47%
    );
  }
  .hair-base {
    position: absolute; inset: 0;
    background: linear-gradient(150deg, #0e0e0e, #262626 60%, #0e0e0e);
  }

  .brow {
    position: absolute;
    top: 44%;
    width: 11%;
    height: 3%;
    background: #161616;
    border-radius: 3px;
  }
  .brow.l { left: 29%; transform: rotate(-3deg); }
  .brow.r { right: 29%; transform: rotate(3deg); }

  .eye {
    position: absolute;
    top: 51%;
    width: 7%;
    aspect-ratio: 1/1.25;
    background: var(--plum);
    border-radius: 50%;
    overflow: hidden;
  }
  .eye::after {
    content: "";
    position: absolute;
    top: 18%; left: 52%;
    width: 32%; height: 32%;
    background: rgba(255, 255, 255, 0.85);
    border-radius: 50%;
  }
  .eye.l { left: 34.5%; }
  .eye.r { right: 34.5%; }

  .nose-line {
    position: absolute;
    left: 50%; top: 60%;
    transform: translateX(-50%);
    width: 8px; height: 12px;
    border-right: 2px solid #e2a670;
    border-bottom: 2px solid #e2a670;
    border-radius: 0 0 3px 0;
  }

  .mouth {
    position: absolute;
    left: 50%; top: 73%;
    transform: translateX(-50%);
    width: 14%;
    height: 2.8%;
    border-bottom: 4px solid var(--plum);
    border-radius: 0 0 40% 40%;
  }

  .tshirt {
    position: absolute;
    left: 50%; bottom: -3%;
    transform: translateX(-50%);
    width: 86%;
    height: 18%;
    background: linear-gradient(160deg, #1c1c1c, #2a2a2a);
    border-radius: 45% 45% 0 0 / 70% 70% 0 0;
    z-index: 2;
  }

  /* Pimples */
  .pimple {
    position: absolute;
    transform: translate(-50%, -50%);
    border-radius: 50%;
    background: radial-gradient(circle at 35% 35%, #fff1f1 0%, #ff5252 50%, #b31d1d 100%);
    border: 2px solid #8c1e1e;
    box-shadow: 0 3px 6px rgba(0, 0, 0, 0.25), inset 0 1px 2px rgba(255, 255, 255, 0.8);
    cursor: pointer;
    animation: pop-in 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    z-index: 10;
    touch-action: manipulation;
  }
  .pimple.small { width: 18px; height: 18px; }
  .pimple.medium { width: 26px; height: 26px; }
  .pimple.large { width: 34px; height: 34px; }
  .pimple::after {
    content: "";
    position: absolute;
    top: 25%; left: 25%;
    width: 30%; height: 30%;
    background: #ffffff;
    border-radius: 50%;
    box-shadow: 0 0 2px #fff;
  }

  @keyframes pop-in {
    from { transform: translate(-50%, -50%) scale(0); }
    to { transform: translate(-50%, -50%) scale(1); }
  }

  .splat {
    position: absolute;
    transform: translate(-50%, -50%);
    pointer-events: none;
    font-family: "Gaegu", sans-serif;
    font-weight: 700;
    color: var(--blush-dark);
    font-size: 18px;
    animation: float-up 0.55s ease-out forwards;
    z-index: 20;
    text-shadow: 0 2px 4px rgba(255,255,255,0.8);
  }
  @keyframes float-up {
    0% { opacity: 1; transform: translate(-50%, -50%) scale(0.8); }
    100% { opacity: 0; transform: translate(-50%, -150%) scale(1.2); }
  }

  .burst {
    position: absolute;
    width: 4px; height: 4px;
    border-radius: 50%;
    background: var(--yellow);
    pointer-events: none;
    z-index: 15;
    animation: particle-fly 0.4s ease-out forwards;
  }
  @keyframes particle-fly {
    0% { opacity: 1; transform: translate(0, 0) scale(1); }
    100% { opacity: 0; transform: translate(var(--dx), var(--dy)) scale(0); }
  }

  #timer-bar-wrap {
    width: 100%;
    height: 10px;
    background: #f0e2d6;
    border-radius: 999px;
    margin-top: 12px;
    overflow: hidden;
  }
  #timer-bar {
    height: 100%;
    background: linear-gradient(90deg, var(--blush), var(--yellow));
    width: 100%;
    transition: width 0.1s linear;
  }

  /* Reaction Stage */
  #reaction-stage {
    position: relative;
    width: 100%;
    aspect-ratio: 3 / 4;
    border-radius: 22px;
    overflow: hidden;
    cursor: pointer;
    user-select: none;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    transition: background 0.12s ease;
  }
  #reaction-stage.waiting { background: linear-gradient(160deg, #ff8e8e, #e84a4a); }
  #reaction-stage.go { background: linear-gradient(160deg, #81f0aa, #22c55e); }
  #reaction-stage.foul { background: linear-gradient(160deg, #e3e3e3, #a3a3a3); }

  #reaction-target {
    width: 140px;
    height: 140px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.25);
    box-shadow: 0 0 0 10px rgba(255, 255, 255, 0.15);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 50px;
    transition: transform 0.12s ease;
  }
  #reaction-stage.go #reaction-target { transform: scale(1.1); }

  #reaction-message {
    margin-top: 24px;
    font-family: "Gaegu", sans-serif;
    font-size: 26px;
    font-weight: 700;
    color: #fff;
    text-shadow: 0 2px 6px rgba(0, 0, 0, 0.25);
    text-align: center;
    padding: 0 10px;
  }

  .reaction-bars {
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin-top: 18px;
    max-height: 180px;
    overflow-y: auto;
  }
  .reaction-bar-row {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 12px;
    color: #6b5c68;
  }
  .reaction-bar-row .r-lbl { width: 48px; flex-shrink: 0; font-weight: 700; }
  .reaction-bar-row .r-track {
    flex: 1;
    height: 12px;
    background: var(--mint-bg);
    border-radius: 999px;
    overflow: hidden;
  }
  .reaction-bar-row .r-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--blush), var(--yellow));
    border-radius: 999px;
  }
  .reaction-bar-row.foul .r-fill { background: #b7b7b7; }
  .reaction-bar-row .r-val { width: 60px; flex-shrink: 0; text-align: right; font-weight: 700; }

  /* End screens */
  .result-num {
    font-family: "Gaegu", sans-serif;
    font-size: 68px;
    font-weight: 700;
    color: var(--blush-dark);
    line-height: 1;
    margin: 6px 0 0;
  }
  .result-lbl { font-size: 13px; color: #8a7c86; font-weight: 700; }
  .stat-row {
    display: flex;
    gap: 10px;
    width: 100%;
    margin-top: 18px;
  }
  .stat {
    flex: 1;
    background: var(--mint-bg);
    border-radius: 14px;
    padding: 10px 4px;
    text-align: center;
  }
  .stat .n { font-weight: 900; font-size: 18px; color: var(--plum); }
  .stat .l { font-size: 11px; color: #6b5c68; }
  .footnote {
    font-size: 11px;
    color: #a89aa3;
    text-align: center;
    margin-top: 18px;
    line-height: 1.5;
  }
</style>
</head>
<body>
<div id="app">
  <div class="eyebrow">POP · POP · SEOU</div>
  <h1>서우 여드름 짜기 챌린지</h1>
  <p class="sub">여드름을 어떻게 만들지 알려주면 AI가 오늘의 서우 얼굴을 준비해 줘요!</p>

  <!-- START SCREEN -->
  <section id="start-screen" class="screen">
    <div class="mode-tabs">
      <button class="mode-tab active" data-mode="timeattack">타임어택 모드</button>
      <button class="mode-tab" data-mode="reaction">반응속도 테스트</button>
    </div>

    <!-- TIME ATTACK PANEL -->
    <div id="timeattack-panel" class="card">
      <label for="prompt-input">오늘의 여드름 컨디션을 입력하세요</label>
      <textarea id="prompt-input" placeholder="예) 이마 위주로 여드름이 잔뜩 났고, 볼에는 커다란 게 몇 개 있어!"></textarea>
      <div class="presets">
        <button class="preset-btn" data-p="이마에 폭발적으로 여드름이 났어! 25개 정도">🌋 이마 폭발형</button>
        <button class="preset-btn" data-p="얼굴 전체에 골고루 자잘한 여드름 15개">✨ 전체 잡티형</button>
        <button class="preset-btn" data-p="턱과 볼 위주로 크고 뾰족한 여드름 10개, 시간 짧게">🎯 턱선 집중형</button>
      </div>
      
      <button id="generate-btn" class="primary-btn">AI에게 여드름 생성 요청하기</button>
      <div id="status-line" class="status-line">AI 생성 및 스마트 스마트 폴백 준비 완료</div>

      <span class="api-key-toggle" onclick="toggleApiKeyBox()">🔑 Gemini API 키 직접 설정 (선택사항)</span>
      <div id="api-key-box" class="api-key-box" hidden>
        <label for="api-key-input">Gemini API Key</label>
        <input type="text" id="api-key-input" placeholder="AIzaSy..." />
        <div style="font-size: 11px; color: #8a7c86; margin-top: 4px;">
          * 입력하지 않아도 지능형 스마트 AI 로직이 자동으로 작동합니다.
        </div>
      </div>
    </div>

    <!-- REACTION PANEL -->
    <div id="reaction-panel" class="card" hidden>
      <label>시도 횟수 선택</label>
      <div class="presets" id="attempt-select">
        <button class="preset-btn attempt-btn" data-count="3">3회</button>
        <button class="preset-btn attempt-btn active" data-count="5">5회</button>
        <button class="preset-btn attempt-btn" data-count="10">10회</button>
        <button class="preset-btn attempt-btn" data-count="15">15회</button>
        <button class="preset-btn attempt-btn" data-count="20">20회</button>
        <button class="preset-btn attempt-btn" data-count="30">30회</button>
      </div>
      <p style="font-size:13px; color:#6b5c68; line-height:1.5; margin-top:10px;">
        얼굴이 <strong style="color:var(--blush-dark);">빨간색</strong>일 때는 대기하세요! <strong style="color:#22c55e;">초록색</strong>으로 변하는 순간 가장 빠르게 터치/클릭하세요.
      </p>
      <button id="reaction-start-btn" class="primary-btn">반응속도 테스트 시작</button>
    </div>
  </section>

  <!-- GAME SCREEN (TIME ATTACK) -->
  <section id="game-screen" class="screen" hidden>
    <div class="hud">
      <div class="pill timer"><span id="hud-time" class="num">30</span><span class="lbl">남은 시간(초)</span></div>
      <div class="pill score"><span id="hud-score" class="num">0</span><span class="lbl">터트린 개수</span></div>
      <div class="pill combo"><span id="hud-combo" class="num">0</span><span class="lbl">콤보</span></div>
    </div>
    <div class="stage-wrap">
      <div id="stage">
        <div class="face">
          <div class="ear l"></div>
          <div class="ear r"></div>
          <div class="neck"></div>
          <div class="head"></div>
          <div class="blush-spot l"></div>
          <div class="blush-spot r"></div>
          <div class="hair-wrap">
            <div class="hair-base"></div>
          </div>
          <div class="brow l"></div>
          <div class="brow r"></div>
          <div class="eye l"></div>
          <div class="eye r"></div>
          <div class="nose-line"></div>
          <div class="mouth"></div>
          <div class="tshirt"></div>
        </div>
        <!-- Pimples dynamically created here -->
      </div>
      <div id="timer-bar-wrap"><div id="timer-bar"></div></div>
    </div>
  </section>

  <!-- REACTION TEST SCREEN -->
  <section id="reaction-screen" class="screen" hidden>
    <div class="hud">
      <div class="pill timer"><span id="reaction-round" class="num">1/5</span><span class="lbl">진행 Round</span></div>
      <div class="pill score"><span id="reaction-last" class="num">-</span><span class="lbl">이번 반응(ms)</span></div>
      <div class="pill combo"><span id="reaction-best" class="num">-</span><span class="lbl">최고 기록(ms)</span></div>
    </div>
    <div class="stage-wrap">
      <div id="reaction-stage" class="waiting">
        <div id="reaction-target">⏳</div>
        <div id="reaction-message">초록색으로 바뀌면 터치하세요!</div>
      </div>
    </div>
  </section>

  <!-- END SCREEN (TIME ATTACK) -->
  <section id="end-screen" class="screen" hidden>
    <div class="card" style="text-align:center;">
      <div class="result-lbl">게임 결과</div>
      <div id="final-score" class="result-num">0</div>
      <div class="result-lbl">개의 여드름을 터트렸습니다!</div>
      <div class="stat-row">
        <div class="stat"><div id="stat-spawned" class="n">0</div><div class="l">생성된 여드름</div></div>
        <div class="stat"><div id="stat-accuracy" class="n">0%</div><div class="l">터트린 비율</div></div>
        <div class="stat"><div id="stat-best-combo" class="n">0</div><div class="l">최대 콤보</div></div>
      </div>
      <button id="retry-btn" class="primary-btn">동일 조건 다시 도전</button>
      <button id="new-prompt-btn" class="primary-btn secondary">새로운 여드름 생성하기</button>
    </div>
  </section>

  <!-- REACTION RESULT SCREEN -->
  <section id="reaction-end-screen" class="screen" hidden>
    <div class="card" style="text-align:center;">
      <div class="result-lbl">평균 반응 속도</div>
      <div id="reaction-avg" class="result-num">0<span style="font-size:22px;">ms</span></div>
      <div id="reaction-rating" class="result-lbl" style="margin-top:2px; color:var(--blush-dark);"></div>
      <div class="stat-row">
        <div class="stat"><div id="reaction-stat-best" class="n">0ms</div><div class="l">최고 기록</div></div>
        <div class="stat"><div id="reaction-stat-worst" class="n">0ms</div><div class="l">최저 기록</div></div>
        <div class="stat"><div id="reaction-stat-fouls" class="n">0회</div><div class="l">성급한 파울</div></div>
      </div>
      <div id="reaction-bars" class="reaction-bars"></div>
      <button id="reaction-retry-btn" class="primary-btn">다시 테스트하기</button>
      <button id="reaction-home-btn" class="primary-btn secondary">메인 화면으로 돌아가기</button>
    </div>
  </section>

  <div class="footnote">가상의 캐릭터 "서우" 얼굴에서 진행되는 재미있는 미니 게임입니다.</div>
</div>

<script>
// --- Web Audio API Synthesizer (No external sound files required) ---
class SoundManager {
  constructor() {
    this.ctx = null;
  }
  init() {
    if (!this.ctx) {
      this.ctx = new (window.AudioContext || window.webkitAudioContext)();
    }
  }
  playPop(size = 'medium') {
    this.init();
    if (!this.ctx) return;
    const osc = this.ctx.createOscillator();
    const gain = this.ctx.createGain();

    let freq = 400;
    if (size === 'small') freq = 600;
    if (size === 'large') freq = 250;

    osc.type = 'sine';
    osc.frequency.setValueAtTime(freq, this.ctx.currentTime);
    osc.frequency.exponentialRampToValueAtTime(80, this.ctx.currentTime + 0.08);

    gain.gain.setValueAtTime(0.6, this.ctx.currentTime);
    gain.gain.exponentialRampToValueAtTime(0.01, this.ctx.currentTime + 0.08);

    osc.connect(gain);
    gain.connect(this.ctx.destination);

    osc.start();
    osc.stop(this.ctx.currentTime + 0.08);
  }
  playSuccess() {
    this.init();
    if (!this.ctx) return;
    const osc = this.ctx.createOscillator();
    const gain = this.ctx.createGain();

    osc.type = 'triangle';
    osc.frequency.setValueAtTime(523.25, this.ctx.currentTime); // C5
    osc.frequency.setValueAtTime(659.25, this.ctx.currentTime + 0.08); // E5
    osc.frequency.setValueAtTime(783.99, this.ctx.currentTime + 0.16); // G5

    gain.gain.setValueAtTime(0.3, this.ctx.currentTime);
    gain.gain.exponentialRampToValueAtTime(0.01, this.ctx.currentTime + 0.3);

    osc.connect(gain);
    gain.connect(this.ctx.destination);

    osc.start();
    osc.stop(this.ctx.currentTime + 0.3);
  }
  playFoul() {
    this.init();
    if (!this.ctx) return;
    const osc = this.ctx.createOscillator();
    const gain = this.ctx.createGain();

    osc.type = 'sawtooth';
    osc.frequency.setValueAtTime(150, this.ctx.currentTime);
    osc.frequency.linearRampToValueAtTime(100, this.ctx.currentTime + 0.2);

    gain.gain.setValueAtTime(0.4, this.ctx.currentTime);
    gain.gain.exponentialRampToValueAtTime(0.01, this.ctx.currentTime + 0.2);

    osc.connect(gain);
    gain.connect(this.ctx.destination);

    osc.start();
    osc.stop(this.ctx.currentTime + 0.2);
  }
}

const sound = new SoundManager();

// --- Main Application Logic ---
(function() {
  // DOM Elements
  const startScreen = document.getElementById('start-screen');
  const gameScreen = document.getElementById('game-screen');
  const endScreen = document.getElementById('end-screen');
  const promptInput = document.getElementById('prompt-input');
  const generateBtn = document.getElementById('generate-btn');
  const statusLine = document.getElementById('status-line');
  const stage = document.getElementById('stage');
  const hudTime = document.getElementById('hud-time');
  const hudScore = document.getElementById('hud-score');
  const hudCombo = document.getElementById('hud-combo');
  const timerBar = document.getElementById('timer-bar');
  const finalScore = document.getElementById('final-score');
  const statSpawned = document.getElementById('stat-spawned');
  const statAccuracy = document.getElementById('stat-accuracy');
  const statBestCombo = document.getElementById('stat-best-combo');
  const retryBtn = document.getElementById('retry-btn');
  const newPromptBtn = document.getElementById('new-prompt-btn');

  const timeattackPanel = document.getElementById('timeattack-panel');
  const reactionPanel = document.getElementById('reaction-panel');
  const reactionScreen = document.getElementById('reaction-screen');
  const reactionEndScreen = document.getElementById('reaction-end-screen');
  const reactionStartBtn = document.getElementById('reaction-start-btn');

  // Mode switching
  document.querySelectorAll('.mode-tab').forEach(tab => {
    tab.addEventListener('click', () => {
      document.querySelectorAll('.mode-tab').forEach(t => t.classList.remove('active'));
      tab.classList.add('active');
      const mode = tab.dataset.mode;
      if (mode === 'timeattack') {
        timeattackPanel.hidden = false;
        reactionPanel.hidden = true;
      } else {
        timeattackPanel.hidden = true;
        reactionPanel.hidden = false;
      }
    });
  });

  // Preset buttons
  document.querySelectorAll('.preset-btn[data-p]').forEach(btn => {
    btn.addEventListener('click', () => {
      promptInput.value = btn.dataset.p;
    });
  });

  // Reaction attempt selection
  let selectedAttemptCount = 5;
  document.querySelectorAll('.attempt-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      document.querySelectorAll('.attempt-btn').forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      selectedAttemptCount = parseInt(btn.dataset.count, 10);
    });
  });

  // --- Game State Variables ---
  let currentGameConfig = null;
  let timerId = null;
  let timeLeft = 30;
  let totalDuration = 30;
  let score = 0;
  let spawnedCount = 0;
  let currentCombo = 0;
  let bestCombo = 0;

  // --- Smart AI & Fallback Generator ---
  function parsePromptWithSmartAI(prompt) {
    const text = prompt.toLowerCase();
    
    // Default region coordinates ranges (% relative to face stage)
    const regionCoords = {
      'forehead': { xMin: 32, xMax: 68, yMin: 22, yMax: 36 },
      'cheek-left': { xMin: 22, xMax: 38, yMin: 52, yMax: 68 },
      'cheek-right': { xMin: 62, xMax: 78, yMin: 52, yMax: 68 },
      'chin': { xMin: 40, xMax: 60, yMin: 76, yMax: 84 },
      'nose': { xMin: 46, xMax: 54, yMin: 54, yMax: 66 }
    };

    let count = 18;
    if (text.includes("많") || text.includes("폭발") || text.includes("잔뜩") || text.includes("가득")) count = 30;
    if (text.includes("적") || text.includes("소수") || text.includes("조금")) count = 10;

    let duration = 30;
    if (text.includes("짧") || text.includes("빨리") || text.includes("급")) duration = 15;
    if (text.includes("길") || text.includes("천천히")) duration = 45;

    // Region weight probabilities
    let weights = { 'forehead': 1, 'cheek-left': 1, 'cheek-right': 1, 'chin': 1, 'nose': 1 };
    
    if (text.includes("이마")) { weights['forehead'] += 4; }
    if (text.includes("볼") || text.includes("볼따구")) { weights['cheek-left'] += 3; weights['cheek-right'] += 3; }
    if (text.includes("턱")) { weights['chin'] += 4; }
    if (text.includes("코")) { weights['nose'] += 3; }

    const spots = [];
    const sizes = ['small', 'medium', 'large'];

    for (let i = 0; i < count; i++) {
      // Pick region based on weight
      const totalWeight = Object.values(weights).reduce((a, b) => a + b, 0);
      let rand = Math.random() * totalWeight;
      let selectedRegion = 'forehead';
      for (const [region, weight] of Object.entries(weights)) {
        if (rand < weight) {
          selectedRegion = region;
          break;
        }
        rand -= weight;
      }

      const box = regionCoords[selectedRegion];
      const x = Math.floor(Math.random() * (box.xMax - box.xMin + 1)) + box.xMin;
      const y = Math.floor(Math.random() * (box.yMax - box.yMin + 1)) + box.yMin;

      let size = sizes[Math.floor(Math.random() * sizes.length)];
      if (text.includes("크") || text.includes("왕")) size = Math.random() > 0.3 ? 'large' : 'medium';
      if (text.includes("작") || text.includes("자잘")) size = Math.random() > 0.3 ? 'small' : 'medium';

      spots.push({ region: selectedRegion, x, y, size });
    }

    return { count, duration, spots };
  }

  // Gemini API Caller
  async function fetchGeminiData(prompt, customApiKey) {
    const apiKey = customApiKey || window.GEMINI_API_KEY;
    if (!apiKey) throw new Error("No Gemini API key available");

    const GEMINI_MODEL = "gemini-2.0-flash";
    const url = `https://generativelanguage.googleapis.com/v1beta/models/${GEMINI_MODEL}:generateContent?key=${apiKey}`;

    const systemInstruction = `너는 "서우 여드름 짜기 챌린지" 게임의 데이터 생성기다.
사용자 설명을 바탕으로 여드름 배치 데이터를 JSON으로 작성하라.
JSON 스키마:
{
  "count": number (5~40),
  "duration": number (15~60),
  "spots": [
    {
      "region": "forehead" | "cheek-left" | "cheek-right" | "chin" | "nose",
      "x": number (20~80),
      "y": number (20~85),
      "size": "small" | "medium" | "large"
    }
  ]
}
오직 JSON만 응답하라.`;

    const requestBody = {
      contents: [{ role: "user", parts: [{ text: `사용자 설명: "${prompt}"` }] }],
      systemInstruction: { role: "system", parts: [{ text: systemInstruction }] },
      generationConfig: { temperature: 0.8, responseMimeType: "application/json" }
    };

    const res = await fetch(url, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(requestBody)
    });

    if (!res.ok) throw new Error(`API Error: ${res.status}`);
    const data = await res.json();
    const rawText = data?.candidates?.[0]?.content?.parts?.[0]?.text;
    if (!rawText) throw new Error("Empty AI response");

    const parsed = JSON.parse(rawText.trim().replace(/^
```json\s*|```$/g, ""));
    return parsed;
  }

  // Generate Button Click
  generateBtn.addEventListener('click', async () => {
    const userPrompt = promptInput.value.trim() || "얼굴 전체에 여드름 15개 생성";
    const customKey = document.getElementById('api-key-input').value.trim();

    generateBtn.disabled = true;
    statusLine.textContent = "AI가 서우의 여드름 상태를 분석 중...";

    try {
      if (customKey) {
        currentGameConfig = await fetchGeminiData(userPrompt, customKey);
        statusLine.textContent = "✨ Gemini API로 맞춤형 여드름 생성 완료!";
      } else {
        // Use smart local AI generator
        await new Promise(r => setTimeout(r, 400)); // smooth experience delay
        currentGameConfig = parsePromptWithSmartAI(userPrompt);
        statusLine.textContent = "⚡ 지능형 AI 로직으로 여드름 생성 완료!";
      }
    } catch (err) {
      console.warn("API Call fallback:", err);
      currentGameConfig = parsePromptWithSmartAI(userPrompt);
      statusLine.textContent = "💡 스마트 폴백 엔진으로 여드름을 생성했습니다!";
    } finally {
      generateBtn.disabled = false;
      setTimeout(startAcneGame, 600);
    }
  });

  // Start Time Attack Game
  function startAcneGame() {
    startScreen.hidden = true;
    endScreen.hidden = true;
    gameScreen.hidden = false;

    // Reset Stage
    stage.querySelectorAll('.pimple, .splat, .burst').forEach(e => e.remove());

    score = 0;
    currentCombo = 0;
    bestCombo = 0;
    spawnedCount = currentGameConfig.spots.length;
    timeLeft = currentGameConfig.duration || 30;
    totalDuration = timeLeft;

    hudScore.textContent = "0";
    hudCombo.textContent = "0";
    hudTime.textContent = timeLeft;
    timerBar.style.width = "100%";

    // Spawn spots
    currentGameConfig.spots.forEach((spot, idx) => {
      const p = document.createElement('div');
      p.className = `pimple ${spot.size || 'medium'}`;
      p.style.left = `${spot.x}%`;
      p.style.top = `${spot.y}%`;
      
      // Gentle staggered entry
      p.style.animationDelay = `${(idx % 6) * 0.05}s`;

      p.addEventListener('pointerdown', (e) => popPimple(p, spot.size, e));
      stage.appendChild(p);
    });

    // Start Timer
    if (timerId) clearInterval(timerId);
    timerId = setInterval(() => {
      timeLeft--;
      hudTime.textContent = timeLeft;
      timerBar.style.width = `${(timeLeft / totalDuration) * 100}%`;

      if (timeLeft <= 0) {
        endAcneGame();
      }
    }, 1000);
  }

  // Pop Pimple Action
  function popPimple(pimpleEl, size, evt) {
    if (pimpleEl.dataset.popped) return;
    pimpleEl.dataset.popped = "true";

    sound.playPop(size);

    score++;
    currentCombo++;
    if (currentCombo > bestCombo) bestCombo = currentCombo;

    hudScore.textContent = score;
    hudCombo.textContent = currentCombo;

    const rect = stage.getBoundingClientRect();
    const x = evt.clientX - rect.left;
    const y = evt.clientY - rect.top;

    // Splat text
    const splat = document.createElement('div');
    splat.className = 'splat';
    splat.textContent = currentCombo > 2 ? `POP! x${currentCombo}` : 'POP!';
    splat.style.left = `${x}px`;
    splat.style.top = `${y}px`;
    stage.appendChild(splat);
    setTimeout(() => splat.remove(), 600);

    // Burst particles
    for (let i = 0; i < 8; i++) {
      const particle = document.createElement('div');
      particle.className = 'burst';
      particle.style.left = `${x}px`;
      particle.style.top = `${y}px`;
      const angle = Math.random() * Math.PI * 2;
      const dist = Math.random() * 30 + 15;
      particle.style.setProperty('--dx', `${Math.cos(angle) * dist}px`);
      particle.style.setProperty('--dy', `${Math.sin(angle) * dist}px`);
      stage.appendChild(particle);
      setTimeout(() => particle.remove(), 400);
    }

    pimpleEl.style.transform = 'translate(-50%, -50%) scale(0.1)';
    pimpleEl.style.opacity = '0';
    setTimeout(() => pimpleEl.remove(), 150);

    // Check if all pimples cleared
    const remaining = stage.querySelectorAll('.pimple:not([data-popped])');
    if (remaining.length === 0) {
      setTimeout(endAcneGame, 300);
    }
  }

  // End Time Attack
  function endAcneGame() {
    clearInterval(timerId);
    gameScreen.hidden = true;
    endScreen.hidden = false;

    finalScore.textContent = score;
    statSpawned.textContent = spawnedCount;
    const acc = spawnedCount > 0 ? Math.round((score / spawnedCount) * 100) : 0;
    statAccuracy.textContent = `${acc}%`;
    statBestCombo.textContent = bestCombo;

    sound.playSuccess();
  }

  retryBtn.addEventListener('click', startAcneGame);
  newPromptBtn.addEventListener('click', () => {
    endScreen.hidden = true;
    startScreen.hidden = false;
  });

  // --- Reaction Speed Test Engine ---
  const reactionStage = document.getElementById('reaction-stage');
  const reactionTarget = document.getElementById('reaction-target');
  const reactionMessage = document.getElementById('reaction-message');
  const reactionRoundLbl = document.getElementById('reaction-round');
  const reactionLastLbl = document.getElementById('reaction-last');
  const reactionBestLbl = document.getElementById('reaction-best');

  let reactionState = 'idle'; // 'idle' | 'waiting' | 'go' | 'finished'
  let reactionRound = 0;
  let reactionTotalRounds = 5;
  let reactionStartTime = 0;
  let reactionTimer = null;
  let reactionResults = [];
  let reactionFouls = 0;

  reactionStartBtn.addEventListener('click', startReactionTest);

  function startReactionTest() {
    reactionTotalRounds = selectedAttemptCount;
    reactionRound = 0;
    reactionResults = [];
    reactionFouls = 0;

    startScreen.hidden = true;
    reactionEndScreen.hidden = true;
    reactionScreen.hidden = false;

    reactionBestLbl.textContent = '-';
    reactionLastLbl.textContent = '-';

    nextReactionRound();
  }

  function nextReactionRound() {
    reactionRound++;
    if (reactionRound > reactionTotalRounds) {
      endReactionTest();
      return;
    }

    reactionRoundLbl.textContent = `${reactionRound}/${reactionTotalRounds}`;
    reactionState = 'waiting';
    reactionStage.className = 'waiting';
    reactionTarget.textContent = '⏳';
    reactionMessage.textContent = '초록색으로 바뀌면 터치하세요!';

    const delay = Math.floor(Math.random() * 2500) + 1500; // 1.5s ~ 4.0s
    if (reactionTimer) clearTimeout(reactionTimer);
    reactionTimer = setTimeout(() => {
      reactionState = 'go';
      reactionStage.className = 'go';
      reactionTarget.textContent = '💥';
      reactionMessage.textContent = '지금 터치하세요!';
      reactionStartTime = performance.now();
    }, delay);
  }

  reactionStage.addEventListener('pointerdown', () => {
    if (reactionState === 'waiting') {
      // Early click / Foul
      clearTimeout(reactionTimer);
      reactionState = 'foul';
      reactionStage.className = 'foul';
      reactionTarget.textContent = '⚠️';
      reactionMessage.textContent = '너무 빨라요! 성급한 클릭입니다.';
      reactionFouls++;
      sound.playFoul();

      setTimeout(() => {
        reactionRound--; // Repeat attempt
        nextReactionRound();
      }, 1200);
    } else if (reactionState === 'go') {
      const elapsed = Math.round(performance.now() - reactionStartTime);
      reactionResults.push(elapsed);
      sound.playPop('large');

      reactionLastLbl.textContent = elapsed;
      const currentBest = Math.min(...reactionResults);
      reactionBestLbl.textContent = currentBest;

      reactionState = 'idle';
      reactionStage.className = 'waiting';
      reactionTarget.textContent = '✨';
      reactionMessage.textContent = `${elapsed}ms! 성공!`;

      setTimeout(nextReactionRound, 1000);
    }
  });

  function endReactionTest() {
    reactionScreen.hidden = true;
    reactionEndScreen.hidden = false;

    const validResults = reactionResults.filter(r => typeof r === 'number');
    const avg = validResults.length > 0 ? Math.round(validResults.reduce((a, b) => a + b, 0) / validResults.length) : 0;
    const best = validResults.length > 0 ? Math.min(...validResults) : 0;
    const worst = validResults.length > 0 ? Math.max(...validResults) : 0;

    document.getElementById('reaction-avg').innerHTML = `${avg}<span style="font-size:22px;">ms</span>`;
    document.getElementById('reaction-stat-best').textContent = `${best}ms`;
    document.getElementById('reaction-stat-worst').textContent = `${worst}ms`;
    document.getElementById('reaction-stat-fouls').textContent = `${reactionFouls}회`;

    let rating = '⚡ 빛의 속도!';
    if (avg > 250) rating = '👍 훌륭한 순발력!';
    if (avg > 350) rating = '🙂 무난한 반응 속도!';
    if (avg > 450) rating = '🐢 조금 더 연습해봐요!';
    document.getElementById('reaction-rating').textContent = rating;

    // Build visual bar graph
    const barsContainer = document.getElementById('reaction-bars');
    barsContainer.innerHTML = '';
    const maxVal = Math.max(...validResults, 500);

    validResults.forEach((time, i) => {
      const row = document.createElement('div');
      row.className = 'reaction-bar-row';
      const fillPercent = Math.min(100, Math.round((time / maxVal) * 100));
      row.innerHTML = `
        <span class="r-lbl">#${i + 1}</span>
        <div class="r-track"><div class="r-fill" style="width:${fillPercent}%;"></div></div>
        <span class="r-val">${time}ms</span>
      `;
      barsContainer.appendChild(row);
    });

    sound.playSuccess();
  }

  document.getElementById('reaction-retry-btn').addEventListener('click', startReactionTest);
  document.getElementById('reaction-home-btn').addEventListener('click', () => {
    reactionEndScreen.hidden = true;
    startScreen.hidden = false;
  });

})();

function toggleApiKeyBox() {
  const box = document.getElementById('api-key-box');
  box.hidden = !box.hidden;
}
</script>
</body>
</html>
```eof

The "서우 여드름 짜기 챌린지" web app is ready! Feel free to test out both the AI time attack mode and reaction speed mode.